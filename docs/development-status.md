# Development status

Windows is the initial implementation. The Windows v1.0 mapping has four self-contained files under `windows/mappings/`; the Mixxx patch remains a separate dependency. The owner subsequently confirmed that the updated composition works in a physical test. Environment and scope details remain to be recorded. Android remains private and is not included. Future platform contributions need their own compatibility, dependency, test and limitation evidence. Publication is subject to the remaining gates.
